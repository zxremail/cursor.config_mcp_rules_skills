---
description: 飞书文档评审：评审意见仅在对话中输出，不直接写入文档评论
alwaysApply: true
---

# 飞书文档评审规范

对飞书文档进行评审、审稿、提意见时：

1. **禁止**使用 `lark-cli drive +add-comment` 或任何 API 直接向文档添加全文/划词评论，除非用户**明确要求**写入文档。
2. 评审意见应在**对话中**以结构化 Markdown 输出（总体评价、分项建议、亮点等）。
3. 用户确认后，方可代为写入文档评论；未确认前默认只展示，不提交。
4. 若用户说「提评审意见」「帮忙 review」，理解为**生成评审内容供其审阅**，而非自动发布到飞书。

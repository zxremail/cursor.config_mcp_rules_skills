---
description: 飞书文档个人默认排版：自动编号标题、浅紫表头、浅蓝首列
alwaysApply: true
---

# 飞书文档个人默认格式

创建或编辑飞书云文档（Docx / Wiki）时，读取并遵守 `~/.cursor/skills/feishu-doc-format/SKILL.md`：

1. **标题**：一律 `seq="auto"`，文本不手写 `1.` / `1.1`；章用 `<h1>`、节用 `<h2>`。
2. **表格**：表头 `rgb(236,226,254)` 且居中；首列数据格 `rgb(225,234,255)`；其余白底。

用户当场指定其他样式时以当场为准。

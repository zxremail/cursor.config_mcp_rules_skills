---
description: 飞书文档个人默认排版：自动编号标题、标题不用代码格式、浅紫表头、浅蓝首列、表头与首列加粗、首列不用代码格式；画板时序图参与者标题栏必须加粗；箭头按向右蓝、向左绿、自调用黑区分
alwaysApply: true
---

# 飞书文档个人默认格式

创建或编辑飞书云文档（Docx / Wiki）时，读取并遵守 `~/.cursor/skills/feishu-doc-format/SKILL.md`：

1. **标题**：一律 `seq="auto"`，文本不手写 `1.` / `1.1`，不用 `<code>`；章用 `<h1>`、节用 `<h2>`。
2. **表格**：表头 `rgb(236,226,254)`、居中、加粗；首列数据格 `rgb(225,234,255)`、加粗、不用 `<code>`；其余白底、常规字重。其它列需要突出命令/标识时可用 `<code>`。
3. **画板时序图**：参与者标题栏字体必须加粗。消息文字保持常规字重。
4. **时序图箭头**：向右请求为实线 `#3370FF`，向左返回为虚线 `#00A870`，自调用为实线 `#1F2329`。自定义色必须设 `border_color_type=1`。流程图步骤箭头不用这组颜色。

用户当场指定其他样式时以当场为准。
